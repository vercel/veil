{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Resource is a consumer's instantiation of a kind.",
  "properties": {
    "dependencies": {
      "description": "Other resources this one depends on. Each entry triggers the target\n kind's `dependent` hook(s) against this resource's bundle during\n render. See SPEC.md \"Dependencies\".",
      "items": {
        "additionalProperties": false,
        "description": "Dependency declares that this resource depends on another resource.\n The depended-on resource (target) authoritatively controls how this\n resource talks to it via the target's `dependent` hooks.",
        "properties": {
          "kind": {
            "description": "The kind of the target resource.",
            "minLength": 1,
            "type": "string"
          },
          "name": {
            "description": "The name of the target resource. The pair (kind, name) must resolve\n to a resource in the catalog at render time.",
            "minLength": 1,
            "type": "string"
          },
          "params": {
            "description": "Arbitrary parameters the target's `dependent` hook receives. The\n shape is defined by the target kind's matching dependents entry.",
            "title": "Struct",
            "type": "object"
          }
        },
        "required": [
          "kind",
          "name"
        ],
        "title": "Dependency",
        "type": "object"
      },
      "type": "array"
    },
    "metadata": {
      "additionalProperties": false,
      "description": "Standard metadata common to all resources.",
      "patternProperties": {
        "^(fileType)$": {
          "description": "What kind of file this metadata block belongs to. Defaults to\n `definition` when omitted (the historical / authoritative\n resource-file shape). Set to `overlay` on overlay fragments so\n the catalog walker can skip them without name/kind heuristics.",
          "enum": [
            "definition",
            "overlay"
          ],
          "title": "Enum",
          "type": "string"
        }
      },
      "properties": {
        "file_type": {
          "description": "What kind of file this metadata block belongs to. Defaults to\n `definition` when omitted (the historical / authoritative\n resource-file shape). Set to `overlay` on overlay fragments so\n the catalog walker can skip them without name/kind heuristics.",
          "enum": [
            "definition",
            "overlay"
          ],
          "title": "Enum",
          "type": "string"
        },
        "kind": {
          "default": "",
          "description": "The name of the kind this resource instantiates. Required when\n `file_type` is `definition`; ignored when `file_type` is\n `overlay` — overlays cannot reassign their target's kind.",
          "type": "string"
        },
        "name": {
          "default": "",
          "description": "The name of this resource. Required when `file_type` is\n `definition` (the default); ignored entirely when `file_type` is\n `overlay` — overlays cannot rename their target resource.",
          "type": "string"
        },
        "overlays": {
          "description": "Conditional overlays applied based on per-variable regex matches.",
          "items": {
            "additionalProperties": false,
            "description": "Overlay is a conditional data merge applied before transforms. The\n overlay applies when every entry in `if` matches: the resolved\n value of each named variable (stringified) must match the\n corresponding Go regular expression.",
            "properties": {
              "file": {
                "description": "Path to the overlay file. Resolved relative to the resource file\n that declares this overlay, or used as-is when absolute.",
                "minLength": 1,
                "type": "string"
              },
              "if": {
                "additionalProperties": {
                  "type": "string"
                },
                "description": "buf:lint:ignore FIELD_LOWER_SNAKE_CASE",
                "propertyNames": {
                  "type": "string"
                },
                "title": "Map of variable name → regex pattern. All entries must match for\n the overlay to apply. An empty map means \"always apply\".",
                "type": "object"
              }
            },
            "required": [
              "file"
            ],
            "title": "Overlay",
            "type": "object"
          },
          "type": "array"
        },
        "overrides": {
          "description": "Source files that have been ejected for local override.",
          "items": {
            "additionalProperties": false,
            "description": "Override maps a kind source file to a local replacement.",
            "properties": {
              "path": {
                "description": "Local path to the override file.",
                "minLength": 1,
                "type": "string"
              },
              "source": {
                "description": "Path of the source file in the kind being replaced.",
                "minLength": 1,
                "type": "string"
              }
            },
            "required": [
              "source",
              "path"
            ],
            "title": "Override",
            "type": "object"
          },
          "type": "array"
        }
      },
      "title": "Metadata",
      "type": "object"
    },
    "spec": {
      "description": "Resource-specific data validated against the kind's schema.",
      "title": "Struct",
      "type": "object"
    }
  },
  "required": [
    "metadata"
  ],
  "title": "Resource",
  "type": "object"
}