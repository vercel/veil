{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "RenderHookDefinition is a single render-hook entry in kind.json: a\n path to the hook source file plus optional `access` declaring the\n host resources the hook may read.",
  "oneOf": [
    {
      "description": "Path to the hook source file. Resolved relative to the enclosing\n kind.json file.",
      "minLength": 1,
      "type": "string"
    },
    {
      "additionalProperties": false,
      "properties": {
        "access": {
          "additionalProperties": false,
          "description": "Optional declaration of host resources this hook needs (env vars,\n and eventually filesystem / network as they're added).",
          "properties": {
            "env": {
              "description": "Environment variables the hook intends to read. Each entry must\n describe both the name and why it's needed; the description is\n surfaced in pre-flight error messages when the var is missing.",
              "items": {
                "additionalProperties": false,
                "description": "EnvAccess names a single host environment variable a hook needs.",
                "properties": {
                  "description": {
                    "description": "A brief description of what this key is used for and why. Surfaced\n when the var is missing so the user can correct the issue.",
                    "minLength": 1,
                    "type": "string"
                  },
                  "name": {
                    "description": "The env var being requested by the hook. Must match the conventions\n for valid POSIX environment-variable identifiers.",
                    "minLength": 1,
                    "pattern": "^[A-Za-z_][A-Za-z0-9_]*$",
                    "type": "string"
                  }
                },
                "required": [
                  "name",
                  "description"
                ],
                "title": "Env Access",
                "type": "object"
              },
              "type": "array"
            }
          },
          "title": "Hook Access",
          "type": "object"
        },
        "path": {
          "description": "Path to the hook source file. Resolved relative to the enclosing\n kind.json file.",
          "minLength": 1,
          "type": "string"
        },
        "source": {
          "default": "",
          "description": "Path referencing this kind's declared `sources` entry this hook\n is bound to. When set, the hook receives (and must return) the\n parsed object for that one source instead of the whole-bundle\n FS: `render(ctx, obj)` in place of `render(ctx, fs)`. Must match\n a `SourceDefinition.path` in the same kind.json's `sources` list.\n Empty (the default) preserves today's whole-bundle contract.",
          "type": "string"
        }
      },
      "required": [
        "path"
      ],
      "type": "object"
    }
  ],
  "title": "Render Hook Definition"
}