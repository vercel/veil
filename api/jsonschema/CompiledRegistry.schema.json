{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "CompiledRegistry is the top-level index written to \u003cout\u003e/registry.json by\n `veil build`. It maps kind name → entry describing how to find that kind's\n compiled artifacts. Consumers (e.g. `veil render`) read this to discover\n which kinds are available in a build artifact.",
  "properties": {
    "kinds": {
      "additionalProperties": {
        "additionalProperties": false,
        "description": "CompiledRegistryEntry describes a single kind in a CompiledRegistry.",
        "properties": {
          "name": {
            "description": "Name of the kind (matches the map key and CompiledKind.name).",
            "minLength": 1,
            "type": "string"
          },
          "path": {
            "description": "Relative path to the compiled kind.json (e.g. \"./worker/kind.json\").",
            "minLength": 1,
            "type": "string"
          },
          "schema": {
            "description": "Relative path to the kind's composite resource schema\n (e.g. \"./worker/kind.schema.json\").",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "name",
          "path",
          "schema"
        ],
        "title": "Compiled Registry Entry",
        "type": "object"
      },
      "description": "Kind name → entry pointing at the compiled kind document and its schema.\n All paths are resolved against the registry.json's own directory.",
      "propertyNames": {
        "type": "string"
      },
      "type": "object"
    }
  },
  "title": "Compiled Registry",
  "type": "object"
}