{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "CompiledHooks mirrors Kind.Hooks but holds compiled/bundled hook entries\n rather than paths.",
  "properties": {
    "render": {
      "description": "Compiled hooks that run during `veil render`, in declaration order.",
      "items": {
        "additionalProperties": false,
        "description": "CompiledHook is a single hook bundled and minified from its TS/JS source,\n with all of its imports resolved and inlined.",
        "properties": {
          "content": {
            "description": "Minified JavaScript bundle, ES module format.",
            "minLength": 1,
            "type": "string"
          },
          "name": {
            "description": "Relative path from the kind directory (e.g. \"hooks/hello-world.ts\").",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "name",
          "content"
        ],
        "title": "Compiled Hook",
        "type": "object"
      },
      "type": "array"
    }
  },
  "title": "Compiled Hooks",
  "type": "object"
}