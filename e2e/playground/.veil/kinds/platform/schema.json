{
  "$schema": "http://json-schema.org/draft-07/schema",
  "description": "A bundle of shared infrastructure a team adopts as one unit. Its dependencies are forwarded, so a service that depends on a platform inherits the platform's database and cache.",
  "type": "object",
  "additionalProperties": false,
  "required": ["owner"],
  "properties": { "owner": { "type": "string" }, "tier": { "type": "string", "default": "standard" } }
}
