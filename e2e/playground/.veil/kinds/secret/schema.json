{
  "$schema": "http://json-schema.org/draft-07/schema",
  "description": "A managed secret. Deliberately registers no dependents: it is internal to whoever provisions it, and must never be inherited by a consumer.",
  "type": "object",
  "additionalProperties": false,
  "required": ["rotationDays"],
  "properties": {
    "rotationDays": { "type": "integer" },
    "rotationLabel": {
      "type": "string",
      "description": "If set, the hook writes this instead — which the source schema rejects, since it demands an integer. Exists so the suite can prove setContent enforces the schema."
    }
  }
}
